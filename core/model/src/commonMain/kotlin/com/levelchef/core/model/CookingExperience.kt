package com.levelchef.core.model

/** How much cooking the user has done — sets the baseline difficulty for recommendations. */
enum class CookingExperience {
    NEVER_COOKED,
    BEGINNER,
    COMFORTABLE,
    CONFIDENT,
    PRO,
}
