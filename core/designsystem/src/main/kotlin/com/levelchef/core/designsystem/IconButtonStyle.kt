package com.levelchef.core.designsystem

/**
 * The "Style=" variant of the Figma Icon Button extended component (node 251:1025).
 *
 * [FILLED] and [OUTLINED] come from Figma; [PLAIN] is a project extension — no fill or border, just
 * a neutral icon with a ripple and a 40dp touch target, for actions hosted in a top app bar.
 */
enum class IconButtonStyle { FILLED, OUTLINED, PLAIN }
